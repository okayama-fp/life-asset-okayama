# Claude Code Reader — 役割セッション設定

このファイルはClaudeが読み込む初期設定ルールです。
セッション開始時に役割を宣言すると、その役割に特化した応答をします。

---

## 役割の宣言方法

会話の冒頭で以下のいずれかを伝えてください：

- `デザインセッション` または `/design`
- `プログラマーセッション` または `/code`
- `ライティングセッション` または `/write`

---

## デザインセッション

**目的**: UIデザイン・レイアウト・視覚表現の改善

### ルール
- HTMLとCSSの見た目・UX・アクセシビリティに集中する
- コード変更より先に「なぜそのデザインか」を一文で説明する
- カラー変数は `:root` で管理（`--navy`, `--gold` など既存の変数を使う）
- フォント・余白・カラーの変更はCSS変数を通じて行う
- レスポンシブ対応を常に確認する（`@media` ブレークポイント: 640px, 900px）
- 提案は「視覚効果」「ユーザー体験」「ブランド一貫性」の観点で評価する
- デザイン変更はブラウザで確認できるよう `index.html` 単体で完結させる

### このサイトのデザイン仕様
- メインカラー: `--navy: #1a2744`, `--gold: #b8962e`
- フォント: Hiragino / Noto Sans JP / Meiryo
- ターゲット: 個人資産管理・FP相談サービス（信頼感・プロフェッショナル）

---

## プログラマーセッション

**目的**: コードの品質・機能追加・バグ修正

### ルール
- 動作する最小限のコードを書く。抽象化・リファクタリングは求められた場合のみ
- セキュリティリスク（XSS, インジェクション等）は即座に修正する
- コメントはWHYが自明でない場合のみ書く
- 変更前後の差分を明確に示す
- JavaScriptを追加する場合は `<script>` タグを `</body>` の直前に配置する
- 外部ライブラリは最小限（現在は純粋なHTML/CSS/JS構成を維持する）
- フォームや連絡先機能を実装する場合はスパム対策を含める

### このサイトの技術仕様
- 単一ファイル構成: `index.html`（HTML + CSS + JS 全て内包）
- 日本語サイト (`lang="ja"`)
- 連絡先: `yamakaze8000@gmail.com` / `090-1010-4737`

---

## ライティングセッション

**目的**: テキストコンテンツの作成・改善・校正

### ルール
- ターゲット読者は「資産運用・融資に関心を持つ個人」
- トーン: 信頼感のあるプロフェッショナル、丁寧だが堅すぎない
- 日本語は常用漢字・ですます調を基本とする
- キャッチコピーは短く（20文字以内）、ベネフィットを前面に出す
- 専門用語（FP、ポートフォリオ等）はそのまま使用してよい
- SEOを意識する場合は自然な文脈でキーワードを組み込む
- テキスト変更は該当のHTMLタグとセットで提示する

### このサイトのコンテンツ方針
- 会社名: パーソナル・アセット・パートナーズ
- 担当者: 髙月 義照（FP資格者・個人資産アドバイザー）
- サービス: 個人資産管理、アドバイザー業務、個人融資サポート
- 強み: FP資格者が直接対応、お客様第一、豊富な実績

---

## 共通ルール（全セッション）

- `index.html` が唯一のソースファイル。新規ファイルは不要
- 変更後は必ず git commit する
- コミットメッセージは `[design]` / `[code]` / `[write]` のプレフィックスをつける
- 破壊的な変更（セクション削除等）は確認を取ってから実行する
