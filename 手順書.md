# ConoHa → Cloudflare 移行手順書

## 必要なもの

| 項目 | 取得場所 |
|------|---------|
| ConoHaサーバーのIPアドレス | ConoHaコントロールパネル > サーバー > IPアドレス |
| CloudflareのAPIトークン | [dash.cloudflare.com](https://dash.cloudflare.com) > マイプロフィール > APIトークン > 「トークンを作成」 |
| Cloudflareのメールアドレス | Cloudflareログインに使っているメール |

## APIトークンの作り方（Cloudflare）

1. https://dash.cloudflare.com にログイン
2. 右上のアイコン → **マイプロフィール**
3. 左メニュー → **APIトークン**
4. **「トークンを作成」** をクリック
5. **「ゾーンを編集」テンプレート** を選択
6. ゾーン権限：`Zone:Edit`, `DNS:Edit` を確認
7. **「概要に進む」→「作成」**
8. 表示されたトークンをコピー（一度しか表示されません）

## 実行手順

```bash
# 1. 依存ライブラリをインストール
pip install requests

# 2. スクリプトを編集（設定欄を入力）
nano cloudflare_migrate.py
# または
vi cloudflare_migrate.py

# 3. 移行スクリプトを実行
python3 cloudflare_migrate.py

# 4. ネームサーバーをConoHaまたはドメイン登録会社で変更
#    （スクリプト実行後に表示されるネームサーバーを設定）

# 5. 反映確認（数時間〜48時間後）
python3 check_dns.py
```

## ネームサーバー変更場所

- **ConoHaで購入したドメイン**: ConoHaコントロールパネル > ドメイン > DNS設定
- **お名前.comで購入**: お名前.com管理画面 > ドメイン設定 > ネームサーバーの変更
- **他のレジストラ**: 各管理画面のDNS/ネームサーバー設定

## 完了後の確認

- `python3 check_dns.py` を実行
- `CF-RAY` ヘッダーが表示されれば移行成功
- WordPressの管理画面 (wp-admin) も正常に開けるか確認

## WordPress追加設定（任意）

Cloudflare経由になった後、WordPressの `wp-config.php` に以下を追加すると
管理画面のSSL警告が出なくなります：

```php
// wp-config.php に追加（<?php の直後）
if (isset($_SERVER['HTTP_X_FORWARDED_PROTO']) && $_SERVER['HTTP_X_FORWARDED_PROTO'] === 'https') {
    $_SERVER['HTTPS'] = 'on';
}
```
